name: Build

on: push

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: composer-installer-action
        uses: nanasess/composer-installer-action@v2.0.1
      - name: Run linter
        run: make lint

